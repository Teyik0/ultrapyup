pre-commit:
  parallel: true
  jobs:
    - name: format
      run: uvx ruff format .
      glob: "*.{py}"
      stage_fixed: true

    - name: lint
      run: uvx ruff check --fix .
      glob: "*.{py}"
      stage_fixed: true

    - name: typecheck
      run: uvx ty check .
      glob: "*.{py}"

pre-push:
  jobs:
    - name: pytest
      run: uv run pytest -n auto tests --cov --cov-report=xml --cov-report=term

commit-msg:
  jobs:
    - name: check
      run: uvx commitlint --file {1}
